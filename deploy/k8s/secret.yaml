apiVersion: v1
kind: Secret
# Example only. Real deployments recreate this secret from environment values.
metadata:
  name: weather-secret
  namespace: weather
type: Opaque
stringData:
  WINDY_WEBCAMS_API_KEY: ""
